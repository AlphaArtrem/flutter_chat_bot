///Class for screen route data
class RouteDetails {
  ///[RouteDetails] default constructor
  RouteDetails(this.name, this.path);

  ///Name of the route
  final String name;

  ///Path of the route
  final String path;
}
